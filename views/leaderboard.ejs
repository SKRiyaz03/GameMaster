<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/leaderboard.css">
    <title>LeaderBoard</title>
</head>
<body>
    <%- include('nav'); -%>
    <div class="leaderboard">
        <h1>LeaderBoard</h1>
        <% for(var i = 0; i < players.length; i++) { %>
            <div class="player-card" id="player-card<%=i%>">
                <h3><%=i+1%></h3>
                 <img src="/avatar/<%=players[i].userName %>" alt="<%= players[i].userName %>">


                <p><%= players[i].displayName %></p>
                <p class = "high-score">High Score: <%= players[i].score %></p>
                <!-- Add any other related content here -->
            </div>
        <%}%>
    </div>
</body>
</html>
